let createStore;_47a‍.x([["increment",()=>increment],["decrement",()=>decrement],["default",()=>_47a‍.o]]);_47a‍.w("redux",[["createStore",["createStore"],function(v){createStore=v}]]);

//ACTION TYPES

const INCREMENT="INCREMENT";
const DECREMENT="DECREMENT";

//ACTION CREATORS
       const increment=(value=1)=>{
    return{//obje
        type:INCREMENT,
        payload:value

    };
};
       const decrement=(value=1)=>({
    type:DECREMENT,
    payload:value
});


const counterReducer= (counter=0,action)=>{
    switch(action.type)
    { case INCREMENT:
        return counter + action.payload ;
     case DECREMENT:
         return  counter - action.payload;

    default :
        return counter
    }
}


_47a‍.d(counterReducer);